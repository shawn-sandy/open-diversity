<nav>
  <div class="container has-text-right">
  {%- for nav in collections.nav | sort(attribute="date") -%}
    <a class="button is-text" href="{{ nav.url | url }}"{% if nav.url == page.url %} data-current="current item"{% endif %}> {{ nav.data.navtitle }} </a>
  {%- endfor -%}
  </div>
</nav>
